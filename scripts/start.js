import splitByWord from "./split-by-word.js"
//updates word array


//controls flow of game from when user clikcs button to generating quote
export default async function start(quote, currentText){
    //start timer
    
    
    
    return QUOTE
}